part of interpolated_countdown;

/// Constants for default interpolation colors used in the countdown widget.
class DefaultInterpolationColors {
  // Outer plate colors
  static const Color outerRingFirstColor = Color(0xFF3BDE86);
  static const Color outerRingMidColor = Color(0xFFF2CB44);
  static const Color outerRingLastColor = Color(0xFFF46666);

  // Inner plate colors
  static const Color innerRingFirstColor = Color(0xFFF2CB44);
  static const Color innerRingMidColor = Color(0xFFF466DE);
  static const Color innerRingLastColor = Color(0xFFFA9334);
}
